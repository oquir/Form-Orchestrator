// Clave propia, aparte del borrador, de los catalogos y de las fechas: descartar un borrador no
// debe llevarse los valores, y son datos de otra naturaleza que los catalogos.
export const VALORES_KEY: string = "form-orchestrator-valores";

// Los nombres de columna con los que arranca el formulario de pegado. Son los del endpoint que ya
// se conoce; si otro los llama distinto se cambian en el panel.
export const DEFAULT_ANIO_KEY: string = "anio";

export const DEFAULT_UVT_KEY: string = "uvt";

export const DEFAULT_SMMLV_KEY: string = "smmlv";
