/** Ancho fijo del menú contextual, usado tanto para posicionar como para el estilo. */
export const MENU_WIDTH_PX: number = 338;

/** Alto estimado del menú, usado para evitar que se salga por abajo del viewport. */
export const MENU_HEIGHT_PX: number = 460;

/** Margen mínimo entre el menú y los bordes del viewport. */
export const VIEWPORT_MARGIN_PX: number = 8;
