// Nombres de columna con los que arranca el formulario de pegado de un catalogo. Viven aca porque
// tanto la logica de parseo (lib/catalogBank) como el hook que arma el formulario
// (hooks/useCatalogCard) los necesitan.
export const DEFAULT_ID_KEY: string = "id";

export const DEFAULT_LABEL_KEY: string = "nombre";
