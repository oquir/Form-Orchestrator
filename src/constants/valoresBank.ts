// Nombres de columna con los que arranca el formulario de pegado de la tabla de UVT y SMMLV.
// Viven aca porque tanto lib/valoresBank como el panel ValoresAnualesEditor los necesitan.
export const DEFAULT_ANIO_KEY: string = "anio";

export const DEFAULT_UVT_KEY: string = "uvt";

export const DEFAULT_SMMLV_KEY: string = "smmlv";
